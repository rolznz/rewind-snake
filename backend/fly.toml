app = "rewind-snake"
primary_region = "iad"

[env]
  SNAKE_DB_PATH = "/data/snake.db"

[build]

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0

[[mounts]]
  source = "snake_data"
  destination = "/data"
